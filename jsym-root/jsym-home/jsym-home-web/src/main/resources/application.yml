spring:
  profiles:
    active: dev
    group:
      common:
        - base
      dev:
        - common
        - dev
      test:
        - common
        - test
      prod:
        - common
        - prod

#サーバー設定
server:
  servlet:
    context-path: /jsym-home
