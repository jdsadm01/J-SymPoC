spring:
  profiles:
    active: dev
    group:
      common:
        - base
      dev:
        - common
        - dev
        - dev-infoservice

#サーバー設定
server:
  servlet:
    context-path: /infoservice
