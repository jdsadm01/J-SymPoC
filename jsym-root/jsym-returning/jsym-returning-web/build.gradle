plugins {
	id 'application-conventions'
}

version = '0.0.2-SNAPSHOT'
bootWar {
	archiveFileName = 'returning.war'
}
dependencies {
	implementation project(':jsym-base')
	implementation project(':jsym-returning:jsym-returning-domain')
	runtimeOnly project(':jsym-comm')
}